{
	"name": "monorepo",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "npm --prefix apps/web run dev",
		"build": "npm --prefix apps/web run build",
		"deploy": "gh-pages -d apps/web/dist"
	},
	"devDependencies": {
		"concurrently": "^9.1.2",
		"gh-pages": "^6.3.0"
	},
	"workspaces": [
		"apps/*"
	]
}
